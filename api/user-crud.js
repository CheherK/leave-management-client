// Define the endpoint for getting a user by ID
export const GET_USER_BY_ID = (id) => "api/users/" + id;

// Define the endpoint for updating a user by ID
export const UPDATE_USER_BY_ID = (id) => "api/users/" + id;

// Define the endpoint for adding a new user
export const ADD_USER_URL = "api/users";

// Define the endpoint for getting a list of all users
export const GET_ALL_USERS_URL = "api/users";
